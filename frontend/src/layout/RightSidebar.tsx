import MissionLogButton from "../features/mission-control/MissionLogButton";

export default function RightSidebar() {
    return (
        <section className="rightbar w-50 flex flex-col">
            <MissionLogButton />
        </section>
    );
}
